# PostgreSQL
POSTGRES_DB=jo2024
POSTGRES_USER=your_db_user
POSTGRES_PASSWORD=your_strong_password

# Frontend build arg (URL accessible depuis le navigateur, pas le nom de service Docker)
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000
